import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stockroom.inventory',
  appName: 'StockRoom',
  webDir: '.output/public'
};

export default config;
